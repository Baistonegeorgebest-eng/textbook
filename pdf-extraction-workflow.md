# PDF教材提取与清洗流程

## 概述

将PDF教材转换为干净的纯文本文件，用于知识库建设。

适用对象：文字型PDF（可选择/复制文字），非扫描件。

## 流程总览

```
PDF原文
  │
  ├─ 1. 类型判断 ─── 扫描件？→ 走OCR流程（本文档不覆盖）
  │
  ├─ 2. 文本提取 ─── PyMuPDF (fitz) 提取全文
  │
  ├─ 3. 清洗修复
  │     ├─ 去页码（独立数字行）
  │     ├─ 去页眉页脚（重复短语）
  │     ├─ 合并空行
  │     ├─ Delta符号统一（∆→Δ）
  │     └─ 常见字体映射修复
  │
  ├─ 4. 元数据注入
  │     ├─ 标题、作者、出版社
  │     ├─ ISBN、页数、语言
  │     └─ 主题标签
  │
  └─ 5. 输出 ─── {书名}.txt
```

## 各步骤详细说明

### Step 1: 类型判断

检查PDF是否为文字型：
- 随机取3-5页，调用 `page.get_text("dict")`
- 如果 text_blocks > 0 → 文字型，可直接提取
- 如果 text_blocks ≈ 0 且有 image blocks → 扫描件，需要OCR

### Step 2: 文本提取

使用 PyMuPDF 逐页提取：

```python
import fitz
doc = fitz.open("input.pdf")
for page in doc:
    text = page.get_text()  # 默认模式，自动处理字体映射
```

注意事项：
- PyMuPDF 版本 ≥ 1.22 对 CM (LaTeX数学) 字体支持较好
- `get_text()` 默认模式已足够，不需要特殊参数
- 提取结果中公式以 Unicode 文本形式存在（α, γ, ε, −, × 等）

### Step 3: 清洗修复

#### 3.1 去页码

PDF中页码通常表现为独立的数字行：

```
...正文内容...
24                          ← 删除这类行
...下一页正文...
```

规则：行内容仅为 1-999 的纯数字 → 删除

#### 3.2 去页眉页脚

页眉页脚特征：每页顶部/底部重复出现的短文本。

检测方法：
1. 取前5页和后5页的首行/末行
2. 统计出现频率 ≥ 3次的短文本（3-30字符）
3. 从全文中删除这些重复行

常见页眉示例：
```
BAINITE IN STEELS          ← 书名
材料科学基础                  ← 书名
CHAPTER 2                   ← 章节名（如果重复出现）
```

#### 3.3 合并空行

连续多个空行 → 保留单个空行

#### 3.4 Delta符号统一

PDF字体映射常见问题：

| 原始字符 | Unicode | 正确字符 | Unicode |
|---------|---------|---------|---------|
| ∆ (Increment) | U+2206 | Δ (Greek Delta) | U+0394 |
| △ (Triangle) | U+25B3 | Δ (Greek Delta) | U+0394 |

替换规则：`∆` → `Δ`，`△` → `Δ`（仅在数学/化学上下文中）

#### 3.5 其他字体映射修复

根据具体PDF可能出现的问题：
- 连字符 `-` (U+002D) vs 减号 `−` (U+2212)
- 弯引号 vs 直引号

### Step 4: 元数据注入

在文件头部添加标准元数据块：

```
# {书名}

**Topic**: {主题标签，逗号分隔}
**Source**: {文件名或来源}
**Author**: {作者}
**Publisher**: {出版社}, {年份}
**ISBN**: {ISBN}
**Pages**: {页数}
**Language**: {语言}

---
```

元数据来源优先级：
1. PDF 内嵌元数据 (`doc.metadata`)
2. 文件名推断
3. 用户手动指定

### Step 5: 输出

输出文件命名：`{原文件名}.txt`
输出编码：UTF-8

## 已知局限

1. **化学式下标**：Fe₃C 会变成 Fe3C，Unicode下标字符无法自动恢复
2. **复杂公式结构**：分数、积分、矩阵等变成线性文本，结构丢失
3. **图片**：仅提取图注文字，图片本身不包含
4. **表格**：结构可能变形，复杂表格需要人工校对
5. **弯引号/特殊标点**：部分字体的特殊字符可能映射不正确

## 质量检查清单

提取完成后，建议检查：

- [ ] 希腊字母是否完整（α, β, γ, δ, ε, θ, σ 等）
- [ ] 公式编号是否保留（如 (2-3), (3-16)）
- [ ] 章节标题是否清晰
- [ ] 参考文献是否完整
- [ ] 无 [公式] 占位符（如果有说明提取工具有问题）
- [ ] 无大量页码残留
- [ ] 无页眉页脚残留
